
import NewStationClient from "./newStationClient";

export const metadata = {
  title: "Create Station | RadiantCastOS",
  description: "Create a new station (placeholder wizard foundation)."
};

export default function NewStationPage() {
  return <NewStationClient />;
}
